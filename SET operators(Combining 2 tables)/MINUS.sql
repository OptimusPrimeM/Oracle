-- MINUS Operator

SELECT * 
FROM SALES;

SELECT * 
FROM SALES_HISTORY;


SELECT 
SALES_DATE, PRODUCT_ID, CUSTOMER_ID
FROM SALES
MINUS
SELECT 
SALES_DATE, PRODUCT_ID, CUSTOMER_ID
FROM SALES_HISTORY;