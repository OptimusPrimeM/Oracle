-- UNION ALL

SELECT * 
FROM SALES;

SELECT * 
FROM SALES_HISTORY;


SELECT 
SALES_DATE, PRODUCT_ID, CUSTOMER_ID
FROM SALES
UNION ALL
SELECT 
SALES_DATE, PRODUCT_ID, CUSTOMER_ID
FROM SALES_HISTORY
ORDER BY SALES_DATE;